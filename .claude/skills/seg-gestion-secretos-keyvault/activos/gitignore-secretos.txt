# ── Secretos y entorno (pegar en el .gitignore del proyecto) ──
.env
.env.*
!.env.example
*.env

# .NET user-secrets viven fuera del repo; por si acaso:
secrets.json
appsettings.*.Local.json

# Credenciales / llaves
*.pem
*.pfx
*.key
*.p12
id_rsa*
.npmrc
.pypirc

# Azure / Terraform state (puede contener secretos en claro)
*.tfstate
*.tfstate.*
.terraform/
azureauth.json

# Vercel
.vercel
